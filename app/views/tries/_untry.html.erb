<div class="<%= dom_id(schedule, :try) %>">
    <div class="flex gap-2">
      <%= link_to schedule_tries_path(schedule_id: schedule.id), class: 'bg-secondary text-white border-2 border-secondary rounded-lg flex', data: { turbo_method: :delete } do %>
          <span class="font-sans">やってみた</span>
      <% end %>
      <%= schedule.tries.count %>
    </div>
</div>
