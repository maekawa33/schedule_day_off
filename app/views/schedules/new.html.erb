<div class="bg-base-200 min-h-screen">
    <div class="flex flex-col gap-4 text-black items-center">
    <h1 class="text-xl md:text-3xl text-black text-bold p-8"><%= t('.title') %></h1>
    <%= render 'form', schedule: @schedule %>
    </div>
</div>
