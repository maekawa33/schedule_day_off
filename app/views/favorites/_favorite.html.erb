<div class="<%= dom_id(schedule, :favorite) %>">
    <div class="flex gap-2">
      <%= link_to schedule_favorites_path(schedule), data: { turbo_method: :post } do %>
        <%= icon(name: "favorite") %>
      <% end %>
      <%= schedule.favorites.count %>
    </div>
</div>
